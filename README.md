Java Indico Client List Workflows Demo
=====================

List workflows from [Indico](https://indicodata.ai/) with Java client.

Set host and Indico api token in `config.properties`, and run:

```
./run.sh
```

You will see the fetched workflows.